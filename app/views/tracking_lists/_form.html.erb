<%= form_with(model: tracking_list, local: true) do |form| %>
  <% if tracking_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tracking_list.errors.count, "error") %> prohibited this tracking_list from being saved:</h2>

      <ul>
        <% tracking_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-4">
    <%= form.label :name%>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
